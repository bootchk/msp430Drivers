High level API.
All use StepperIndexer (a driver chip that implements stepping.)

StandbyStepper is powered off when not being used.

StepperMotor API is in units of steps.

DegreeStepper API is in units of degrees.